# Core — ontology loading, SPARQL, the chat-with-graph pipeline, and the
# hurricane loss-normalization notebook.
rdflib>=7.0
pandas>=2.0
tabulate>=0.9
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
jupyter>=1.0
openpyxl>=3.1          # read the .xlsx data files with pandas

# LLM backends for "chat with your graph" — install only the one you use.
# langchain-openai>=0.1     # chatWithGraph_OpenAI.py     (needs OPENAI_API_KEY)
# anthropic>=0.40           # chatWithGraph_anthropic.py  (needs ANTHROPIC_API_KEY; billable)
# chatWithGraph_claude.py uses the local `claude` CLI on PATH — no package, no key.
